# Chat Service — Project Rules

## Server Lifecycle
- **Always stop the server** immediately after starting it for any verification, testing, or smoke-check.
  - Start the server → check the output → kill it right away.
  - Never leave a server running as a background task unless the user explicitly asks to keep it running.
